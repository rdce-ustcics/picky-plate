# --- App ---
PORT=4000
NODE_ENV=development

# --- Mongo ---
MONGO_URI=mongodb://127.0.0.1:27017/pickaplate

# --- JWT (used for login & optional auto-login after verify) ---
JWT_ACCESS_SECRET=kweydryanmarcusyrampickaplatesecret
JWT_ACCESS_TTL_MIN=60

# --- OTP ---
OTP_CODE_TTL_MIN=10
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SEC=60

# --- SMTP (use Gmail App Password, Mailtrap, or SES SMTP) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply.pickaplate@gmail.com
SMTP_PASS=dyzzlmqoqiiuevge
MAIL_FROM="PickAPlate <noreply.pickaplate@gmail.com>"


# --- CORS (frontend URL) ---
CORS_ORIGIN=http://localhost:5173

# --- Google Cloud Vision API (Image Moderation) ---
# Option 1: Path to service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=./config/google-cloud-key.json
# Option 2: Set project ID if using default credentials
# GOOGLE_CLOUD_PROJECT=your-project-id
